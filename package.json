{
  "name": "keyquest-admin",
  "version": "1.0.0",
  "description": "Keyquest Mortgage Admin Panel",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "node inject-env.js && vite build",
    "preview": "vite preview",
    "serve": "python -m http.server 8000",
    "prebuild": "node inject-env.js"
  },
  "devDependencies": {
    "vite": "^4.5.0",
    "dompurify": "^3.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  }
}